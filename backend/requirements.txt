# SynaptDI backend dependencies
fastapi>=0.110
uvicorn[standard]>=0.27
chromadb==1.5.9          # behaviour is tuned to this version (cosine space, HNSW)
pydantic>=2.5
requests>=2.31
pymupdf>=1.24            # PDF text extraction (imported as `fitz`)
pyyaml>=6.0              # parsing OpenAPI YAML specs
python-multipart>=0.0.9  # REQUIRED by FastAPI for file uploads
python-docx>=1.1         # .docx parsing
openpyxl>=3.1            # .xlsx parsing
python-pptx>=0.6         # .pptx parsing
